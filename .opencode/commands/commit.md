---
description: Commit changes with documentation verification and Conventional Commits
---

Load the `commit-workflow` skill and follow its full workflow to commit the current changes.

Verify all documentation is up to date before staging, handle feature-branch creation if on main, use Conventional Commits format, and run lint before finalizing.
