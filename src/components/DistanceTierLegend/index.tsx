/**
 * DistanceTierLegend index
 * 
 * WHY: Simple re-export - no dynamic import needed since this is
 * a pure React component without browser-specific APIs like Leaflet.
 */

export { default } from './DistanceTierLegend';
